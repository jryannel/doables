# Doables – Customer Research

This is the customer research for Doables, run with the `customer-avatars` plugin.
Start with `/customer-avatars:status` for an overview.

- `private/` and `demo/accounts.md` are never versioned (see `.gitignore`).
- Interviews require the participant's consent to recording and to AI-assisted analysis.
